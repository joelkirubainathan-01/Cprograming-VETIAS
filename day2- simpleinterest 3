#include <stdio.h>
int main() {
    float X, time, interest;
    printf("Enter principal amount (X): ");
    scanf("%f", &X);
    printf("Enter time in years: ");
    scanf("%f", &time);
    interest = (X * time * 10) / 100;
    printf("The Simple Interest is: %.2f\n", interest);
    return 0;
}
